import type { Metadata } from "next";
import { Geist, Geist_Mono, Playfair_Display } from "next/font/google";
import "./globals.css";
import { BasketProvider } from "@/app/context/basket-context";
import RootContent from "@/app/components/root-content";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-serif",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "4050 - Homegrown and Heartfelt",
  description: "Pure, simple ingredients grown in our backyard and preserved with generations of care. 100% of profits support community causes.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${playfair.variable} antialiased`}
      >
        <BasketProvider>
          <RootContent>{children}</RootContent>
        </BasketProvider>
      </body>
    </html>
  );
}
